# AI boundaries

The prototype uses deterministic demo responses so it never fabricates live marketplace data. Confidence and evidence are shown with recommendations. Production should add provider adapters for multilingual ASR, structured extraction and explanations; all output must be validated before persistence and low-confidence fields must remain human-confirmed.
