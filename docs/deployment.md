# Deployment

Run with the included Dockerfile on Render, Railway, Fly.io or a small VPS. Set `SECRET_KEY`, `DATABASE_URL`, `DEMO_MODE=false` and provider credentials through the platform secret manager. Use PostgreSQL for production and a managed object store for uploads.
